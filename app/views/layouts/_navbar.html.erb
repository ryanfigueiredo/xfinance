<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 field-filter">
        <% ['Dashboard', 'Tags', 'Payers'].map do |item| %>
          <li class="nav-item">
            <%= link_to(item, "/#{item.downcase}", { class: 'nav-link active' }) %>
          </li>
        <% end %>
      </ul>

      <div class="fields-form-add-finance-transaction" hidden>
        <%= render "finance_transactions/form", finance_transaction: @finance_transaction %>
      </div>

      <%= render "layouts/filter_by" %>
    </div>
  </div>
</nav>

<%= render "layouts/nav_months" %>
